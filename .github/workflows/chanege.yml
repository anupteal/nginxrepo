name: Generate changelog
on:
  pull_request:
    branches: [ master ]
  release:
    types: [created, edited]

jobs:
    build:
        # ...
        steps:
            # ...
            - name: Import Secrets
              uses: hashicorp/vault-action@v2.1.2
              with:
                url: https://3.90.180.170/.com:8200
                method: approle
                roleId: ${{ secrets.roleId }}
                secretId: ${{ secrets.secretId   }}
                secrets: |
                    secret/eng/api/ new            
